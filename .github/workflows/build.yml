name: Build & Verify
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: archlinux
    steps:
    - uses: actions/checkout@v2
    - uses: kruhlmann/dotfiles@v1
    - name: Install dotfiles
